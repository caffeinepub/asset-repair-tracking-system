{
  "frontend/src/App.tsx": "Main app entry point wrapping with ThemeProvider, QueryClientProvider, and AuthProvider; shows LoginScreen when unauthenticated, otherwise renders the TanStack Router with all page routes including settings.",
  "frontend/src/components/Sidebar.tsx": "Left sidebar navigation that gates the Settings link on the backend-verified principal admin role from useGetCallerRole, replacing any local session-based role checks.",
  "frontend/src/components/PartsReplacedEditor.tsx": "Inline editor for the parts-replaced array in repair tickets, with separate typed handler functions (`updatePartNumber`, `updateQty`) to fix the `string | bigint` type error on `partName`.",
  "frontend/src/components/Layout.tsx": "Shell layout component combining Sidebar and Header with a scrollable main content area, accepting optional children or rendering the router Outlet.",
  "frontend/src/pages/RepairTicketsPage.tsx": "Repair tickets list page with search/stage filter and table; fixed `NewRepairTicketForm` and `RepairTicketDetailModal` usages to pass `onClose` instead of `onOpenChange` to match their prop interfaces.",
  "frontend/src/pages/PartsInventoryPage.tsx": "Parts inventory page with a new Image thumbnail column (48×48px with Package placeholder for parts without images), an Edit Part dialog supporting image upload/replace/remove, and the existing Add Part dialog updated with image upload; also includes low stock alerts, search, stock adjustment modal, and admin-only controls.",
  "frontend/src/pages/ReportsPage.tsx": "Reports and analytics page with repair frequency, parts charts, technician workload, and yearly alerts; now imports `useTheme` and applies lime green (`#84cc16`) to all Recharts axis ticks, grid strokes, and tooltip text in dark mode, leaving light mode colors unchanged.",
  "frontend/src/pages/AuditLogPage.tsx": "Audit log page with paginated table and search; fixed `useGetAuditTrail` call to pass required `null, null, null` arguments.",
  "frontend/src/pages/SettingsPage.tsx": "Admin-only settings page with user management table and client registry; access is gated on the backend-verified principal role from useGetCallerRole, with clear error display for authorization failures.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for all backend operations; all BigInt values in query keys are converted with `.toString()` to satisfy the linter, and backward-compatibility aliases are exported for all renamed hooks.",
  "frontend/src/components/AddAssetForm.tsx": "Dialog form for registering new assets, with the Client field replaced by a dynamic Select dropdown populated from `useGetClients` with loading state indicator.",
  "frontend/src/components/EditAssetForm.tsx": "Dialog form for editing existing assets; fixed `useUpdateAsset` mutation call to use `asset` key instead of `updatedAsset`.",
  "frontend/src/pages/AssetsPage.tsx": "Assets list page with search/filter, CRUD table, import functionality, and a new Export dropdown button (beside Import) offering \"Export as Excel (.xlsx)\" and \"Export as CSV (.csv)\" options; export respects active search/filter and is available to all authenticated users, while Import and Add Asset remain admin-only.",
  "frontend/src/components/AssetDetailModal.tsx": "Asset detail dialog showing asset info grid and repair history table; updated to use default import of StatusBadge and added default export.",
  "frontend/src/components/RepairTicketDetailModal.tsx": "Repair ticket detail/edit dialog with parts replaced editor; updated to use default import of StatusBadge, made `canEdit` optional, and added default export.",
  "frontend/src/components/NewRepairTicketForm.tsx": "Dialog form for creating new repair tickets with asset selector, technician, fault description, diagnosis, outcome, and parts editor; fixed the missing `currentStage` field by defaulting new tickets to `'Received'`.",
  "frontend/src/components/StockAdjustmentModal.tsx": "Modal for adjusting part stock levels with add/remove operations; added default export alongside named export.",
  "frontend/src/pages/DashboardPage.tsx": "Dashboard overview page with KPI cards, a new two-column charts row (RepairsByStageChart + TopFailingModelsChart) positioned below KPI cards and above the recent records tables, plus the existing low stock alert section.",
  "frontend/tailwind.config.js": "Tailwind CSS configuration with class-based dark mode, OKLCH color tokens for all semantic colors including sidebar/success/warning, custom fonts, border radius, and animations; uses synchronous `require()` for plugins to be compatible with the jiti CommonJS loader.",
  "frontend/src/components/RepairsByStageChart.tsx": "Vertical green bar chart of repair counts by stage using Recharts; now imports `useTheme` and applies lime green (`#84cc16`) to all axis tick labels, grid lines, and tooltip text when dark mode is active, with default muted colors in light mode.",
  "frontend/src/components/TopFailingModelsChart.tsx": "Horizontal orange bar chart of repair counts per device model using Recharts; now imports `useTheme` and applies lime green (`#84cc16`) to all axis tick labels, grid lines, and tooltip text when dark mode is active, with default muted colors in light mode.",
  "frontend/src/utils/fileParser.ts": "Utility module for parsing .xlsx, .csv, and .txt files to extract serial numbers; uses SheetJS loaded from CDN for Excel files, native text parsing for CSV/TXT, always reads the first column, and auto-detects/skips header rows.",
  "frontend/src/components/ImportPreviewModal.tsx": "Preview dialog shown after file parsing that lists all extracted serial numbers, highlights duplicates with warning styling and icons, shows a summary count of new vs duplicate entries, and provides Confirm/Cancel actions.",
  "frontend/src/components/ImportSummaryModal.tsx": "Post-import summary dialog displaying color-coded counts of successfully imported serials (green), skipped duplicates with warnings (yellow), and any errors (red) after a batch import completes.",
  "frontend/src/contexts/AuthContext.tsx": "AuthContext managing authenticated user state; login flow stores pending credentials in refs and triggers II authentication reactively via useEffect, avoiding `.catch()` on the void-returning `iiLogin()`, with null-safe reject calls.",
  "frontend/src/components/LoginScreen.tsx": "Login page with username/password fields; calls `login(actor, username, password)` with the actor as the first argument to match the updated AuthContext signature.",
  "frontend/src/components/CreateUserDialog.tsx": "Dialog form for creating a new managed user; uses the useCreateUser mutation hook (authenticated actor) with inline error display and toast notifications.",
  "frontend/src/components/EditUserDialog.tsx": "Dialog form for editing an existing managed user; uses the useUpdateUser mutation hook (authenticated actor) with inline error display and optional password change support.",
  "frontend/src/components/Header.tsx": "Top header bar showing app logo, authenticated user name, a principal-verified role badge fetched from the backend via useGetCallerRole, a theme toggle, and a logout button.",
  "frontend/src/utils/csvExport.ts": "Export utility with `exportAssetsAsCSV` and `exportAssetsAsExcel` functions using industry-standard columns (Serial Number, Asset Name, Category, Status, Date Added, Assigned User), plus legacy `exportAssetsToCSV` and `exportRepairsToCSV` functions. Fixed the TypeScript error by replacing the generic `downloadFile(content: string | Uint8Array, ...)` with a string-only `downloadCSVFile(content: string, ...)` since SheetJS handles its own file download via `XLSX.writeFile`.",
  "frontend/src/components/AddPartForm.tsx": "Dialog form for adding a new part with file upload support (JPEG/PNG/WebP, max 2MB), image preview with remove button, client-side validation, and Uint8Array encoding before submitting to the backend.",
  "frontend/src/components/ClientRegistrySection.tsx": "Extracted client registry card component with add, rename, and delete client functionality using the useAddClient, useRenameClient, and useDeleteClient mutation hooks with full error handling and toast notifications."
}